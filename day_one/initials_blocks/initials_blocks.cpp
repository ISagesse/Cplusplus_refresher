#include <iostream>
#include <string>

using namespace std;

int main()
{
	string initial_val = "I";

	cout << initial_val + initial_val + initial_val + initial_val + initial_val << endl;
	cout << initial_val << endl;
	cout << initial_val + initial_val + initial_val + initial_val << endl;
	cout << initial_val << endl;
	cout << initial_val << endl;

	return 0;
}

